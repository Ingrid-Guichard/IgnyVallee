<form method="post" action="{{ path('activite_verger_deleteImage', {'id': image.id}) }}" onsubmit="return confirm('Etes-vous sûrs de ne plus vouloir utiliser cette photo pour votre site ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">
    <button class="btn button_supp_event mt-2" >
        <img src="{{ asset('/build/images/supprimer.svg') }}" class="button_supp_miniature"  alt="Supprimer">
    </button>
</form>
