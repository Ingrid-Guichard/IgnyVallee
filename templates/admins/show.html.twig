{% extends 'base.html.twig' %}

{% block title %}Admins{% endblock %}

{% block body %}
    <h1>Admins</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>HeuresGestionAdmin</th>
                <td>{{ admin.heuresGestionAdmin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admins_index') }}">back to list</a>

    <a href="{{ path('admins_edit', {'id': admin.id}) }}">edit</a>

    {{ include('admins/_delete_form.html.twig') }}
{% endblock %}
