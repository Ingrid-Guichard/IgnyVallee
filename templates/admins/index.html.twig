{% extends 'base.html.twig' %}

{% block title %}Admins index{% endblock %}

{% block content %}
    <h1>Admins index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>HeuresGestionAdmin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for admin in admins %}
            <tr>
                <td>{{ admin.id }}</td>
                <td>{{ admin.heuresGestionAdmin }}</td>
                <td>
                    <a href="{{ path('admins_show', {'id': admin.id}) }}">show</a>
                    <a href="{{ path('admins_edit', {'id': admin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admins_new') }}">Create new</a>
{% endblock %}
