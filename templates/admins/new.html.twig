{% extends 'base.html.twig' %}

{% block title %}Nouvel Administrateur{% endblock %}

{% block content %}

    <div class="formulaire mr-4 ml-4">
        <div class="card border-danger mb-3 mt-5" >
            <div class="card-header">
                <h1 class="mt-2">
                    <a href="{{ path('admins_index') }}">
                        <img src="{{ asset('/build/images/return.svg') }}" class="button_return_index"  alt="Retour">
                    </a>
                    Créer un nouvel administrateur
                </h1>
            </div>
            <div class="card-body text-danger">

                {{ include('admins/_form.html.twig') }}

            </div>
        </div>
    </div>

{% endblock %}
