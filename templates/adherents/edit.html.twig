{% extends 'base.html.twig' %}

{% block title %}Modifier un adhérent{% endblock %}

{% block content %}

    <div class="formulaire mr-4 ml-4">
        <div class="card border-danger mb-3 mt-5" >
            <div class="card-header">
                <h1 class="mt-2">
                    <a href="{{ path('adherents_index') }}">
                        <img src="{{ asset('/build/images/return.svg') }}" class="button_return_index"  alt="Retour">
                    </a>
                    {{ include('adherents/_delete_form.html.twig') }}
                    Modification de {{ adherent.prenom|capitalize }} {{ adherent.nom|upper }}
                </h1>
            </div>
            <div class="card-body text-danger">

                {{ include('adherents/_formEdit.html.twig', {'button_label': 'Update'}) }}

            </div>
        </div>
    </div>
{% endblock %}
